<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xingji.frameproject.mybatis.dao.SysRoleDao">

    <resultMap type="com.xingji.frameproject.mybatis.entity.SysRole" id="SysRoleMap">
        <result property="roleId" column="role_id" jdbcType="INTEGER"/>
        <result property="roleName" column="role_name" jdbcType="VARCHAR"/>
        <result property="founder" column="founder" jdbcType="VARCHAR"/>
        <result property="foundTime" column="found_time" jdbcType="TIMESTAMP"/>
        <result property="delFlag" column="del_flag" jdbcType="INTEGER"/>
        <result property="updatedBy" column="updated_by" jdbcType="VARCHAR"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="SysRoleMap">
        select
          role_id, role_name, founder, found_time, del_flag, updated_by, update_time
        from frameproject.sys_role
        where role_id = #{roleId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SysRoleMap">
        select
          role_id, role_name, founder, found_time, del_flag, updated_by, update_time
        from frameproject.sys_role
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="SysRoleMap">
        select
        role_id, role_name, founder, found_time, del_flag, updated_by, update_time
        from frameproject.sys_role
        <where>
            <if test="roleId != null">
                and role_id = #{roleId}
            </if>
            <if test="roleName != null and roleName != ''">
                and role_name = #{roleName}
            </if>
            <if test="founder != null and founder != ''">
                and founder = #{founder}
            </if>
            <if test="foundTime != null">
                and found_time = #{foundTime}
            </if>
            <if test="delFlag != null">
                and del_flag = #{delFlag}
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                and updated_by = #{updatedBy}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="roleId" useGeneratedKeys="true">
        insert into frameproject.sys_role(role_name, founder, found_time, del_flag, updated_by, update_time)
        values (#{roleName}, #{founder}, #{foundTime}, #{delFlag}, #{updatedBy}, #{updateTime})
    </insert>

    <insert id="insertBatch" keyProperty="roleId" useGeneratedKeys="true">
        insert into frameproject.sys_role(role_name, founder, found_time, del_flag, updated_by, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.roleName}, #{entity.founder}, #{entity.foundTime}, #{entity.delFlag}, #{entity.updatedBy},
            #{entity.updateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="roleId" useGeneratedKeys="true">
        insert into frameproject.sys_role(role_name, founder, found_time, del_flag, updated_by, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.roleName}, #{entity.founder}, #{entity.foundTime}, #{entity.delFlag}, #{entity.updatedBy},
            #{entity.updateTime})
        </foreach>
        on duplicate key update
        role_name = values(role_name) , founder = values(founder) , found_time = values(found_time) , del_flag =
        values(del_flag) , updated_by = values(updated_by) , update_time = values(update_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update frameproject.sys_role
        <set>
            <if test="roleName != null and roleName != ''">
                role_name = #{roleName},
            </if>
            <if test="founder != null and founder != ''">
                founder = #{founder},
            </if>
            <if test="foundTime != null">
                found_time = #{foundTime},
            </if>
            <if test="delFlag != null">
                del_flag = #{delFlag},
            </if>
            <if test="updatedBy != null and updatedBy != ''">
                updated_by = #{updatedBy},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where role_id = #{roleId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from frameproject.sys_role where role_id = #{roleId}
    </delete>

   <select id="queryRoleNameByroleId" resultType="java.lang.String">
      select role_name
        from sys_role
        where role_id=#{roleId,jdbcType=INTEGER}
    </select>
    <!--查询用户所属角色-->
    <select id="userhasrole" resultMap="SysRoleMap">
         select c.* from sys_user a
         inner join sys_user_role b on a.user_id=b.user_id
				INNER JOIN sys_role c on c.role_id=b.role_id
				WHERE a.user_id=#{userId}
    </select>
</mapper>

