<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xingji.frameproject.mybatis.dao.PurchaseReturnsDetailsDao">
    <!-- 结果集 -->
    <resultMap type="com.xingji.frameproject.mybatis.entity.PurchaseReturnsDetails" id="PurchaseReturnsDetailsMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="returnId" column="return_id" jdbcType="VARCHAR"/>
        <result property="productName" column="product_name" jdbcType="VARCHAR"/>
        <result property="productId" column="product_id" jdbcType="VARCHAR"/>
        <result property="returnNum" column="return_num" jdbcType="INTEGER"/>
        <result property="productUnit" column="product_unit" jdbcType="VARCHAR"/>
        <result property="purchaseUnitPrice" column="purchase_unit_price" jdbcType="NUMERIC"/>
        <result property="purchaseMoney" column="purchase_money" jdbcType="NUMERIC"/>
        <result property="depotName" column="depot_name" jdbcType="VARCHAR"/>
        <result property="ingredient" column="ingredient" jdbcType="VARCHAR"/>
        <result property="gramHeavy" column="gram_heavy" jdbcType="VARCHAR"/>
        <result property="productDescribe" column="product_describe" jdbcType="VARCHAR"/>

    </resultMap>




    <!-- 基本字段 -->
    <sql id="Base_Column_List">
        id
        , return_id, product_id, return_num, purchase_money, depot_name    </sql>
    <sql id="Base_Column">
        p.id
        , p.return_id, p.product_id, p.return_num, p.purchase_money, p.depot_name    </sql>

    <!-- 查询单个 -->
    <select id="selectById" resultMap="PurchaseReturnsDetailsMap">
        select
        <include refid="Base_Column_List"/>
        from purchase_returns_details
        where id = #{id}
    </select>

    <!-- 查询全部 -->
    <select id="selectAll" resultMap="PurchaseReturnsDetailsMap">
        select
        <include refid="Base_Column_List"/>
        from purchase_returns_details
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="selectList" resultMap="PurchaseReturnsDetailsMap">
        select
        <include refid="Base_Column_List"/>
        from purchase_returns_details
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="returnId != null and returnId != ''">
                and return_id = #{returnId}
            </if>
            <if test="productId != null and productId != ''">
                and product_id = #{productId}
            </if>
            <if test="returnNum != null">
                and return_num = #{returnNum}
            </if>
            <if test="purchaseMoney != null">
                and purchase_money = #{purchaseMoney}
            </if>
            <if test="depotName != null and depotName != ''">
                and depot_name = #{depotName}
            </if>
        </where>
    </select>

    <!-- 新增所有列 -->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into purchase_returns_details(id, return_id, product_id, return_num, purchase_money, depot_name)
        values (#{id}, #{returnId}, #{productId}, #{returnNum}, #{purchaseMoney}, #{depotName})
    </insert>

    <!-- 批量新增 -->
    <insert id="batchInsert">
        insert into purchase_returns_details(return_id, product_id, return_num, purchase_money, depot_name)
        values
        <foreach collection="entities" item="item" index="index" separator=",">
            (
             #{item.returnId}, #{item.productId}, #{item.returnNum}, #{item.purchaseMoney}, #{item.depotName}
            )
        </foreach>
    </insert>

    <!-- 通过主键修改数据 -->
    <update id="update">
        update frameproject.purchase_returns_details
        <set>
            <if test="returnId != null and returnId != ''">
                return_id = #{returnId},
            </if>
            <if test="productId != null and productId != ''">
                product_id = #{productId},
            </if>
            <if test="returnNum != null">
                return_num = #{returnNum},
            </if>
            <if test="purchaseMoney != null">
                purchase_money = #{purchaseMoney},
            </if>
            <if test="depotName != null and depotName != ''">
                depot_name = #{depotName},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from purchase_returns_details
        where id = #{id}
    </delete>

    <!-- 总数 -->
    <select id="count" resultType="int">
        select count(*)
        from purchase_returns_details
    </select>

    <select id="queryAllByOrderId" resultMap="PurchaseReturnsDetailsMap">
        select
        <include refid="Base_Column"/>,b.product_name,c.unit_name,b.purchase_unit_price,b.ingredient,b.gram_heavy,b.product_describe
        from purchase_returns_details p
        INNER JOIN base_product b on p.product_id=b.product_id
        INNER JOIN base_unit c on b.unit_id=c.unit_id
        where return_id=#{id}
    </select>
</mapper>
