<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xingji.frameproject.mybatis.dao.PurchaseOrderDetailsDao">
    <!-- 结果集 -->
    <resultMap type="com.xingji.frameproject.mybatis.entity.PurchaseOrderDetails" id="PurchaseOrderDetailsMap">
    <result property="id" column="id" jdbcType="INTEGER"/>
    <result property="purchaseOrderId" column="purchase_order_id" jdbcType="VARCHAR"/>
    <result property="productName" column="product_name" jdbcType="VARCHAR"/>
    <result property="productId" column="product_id" jdbcType="VARCHAR"/>
    <result property="productNum" column="product_num" jdbcType="INTEGER"/>
    <result property="productUnit" column="unit_name" jdbcType="VARCHAR"/>
    <result property="purchaseUnitPrice" column="purchase_unit_price" jdbcType="NUMERIC"/>
    <result property="purchaseMoney" column="purchase_money" jdbcType="NUMERIC"/>
    <result property="depotName" column="depot_name" jdbcType="VARCHAR"/>
    <result property="remark" column="remark" jdbcType="VARCHAR"/>
    <result property="ingredient" column="ingredient" jdbcType="VARCHAR"/>
    <result property="gramHeavy" column="gram_heavy" jdbcType="VARCHAR"/>
    <result property="productDescribe" column="product_describe" jdbcType="VARCHAR"/>
    </resultMap>
    <!-- 基本字段 -->
    <sql id="Base_Column_List">
        p.id
        , p.purchase_order_id, p.product_id, p.product_num, p.purchase_money, p.depot_name, p.remark    </sql>

    <!-- 查询单个 -->
    <select id="queryById" resultMap="PurchaseOrderDetailsMap">
        select
        <include refid="Base_Column_List"/>
        from purchase_order_details
        where id = #{id}
    </select>

    <!-- 通过采购订单进行查询订单详情 -->
    <select id="queryAllByOrderId" resultMap="PurchaseOrderDetailsMap">
        select
        <include refid="Base_Column_List"/>,b.product_name,c.unit_name,b.purchase_unit_price,b.ingredient,b.gram_heavy,b.product_describe
        from purchase_order_details p
        INNER JOIN base_product b on p.product_id=b.product_id
        INNER JOIN base_unit c on b.unit_id=c.unit_id
        where purchase_order_id = #{orderid}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryOrByPojo" resultMap="PurchaseOrderDetailsMap">
        select
        <include refid="Base_Column_List"/>
        from purchase_order_details p
        <where>
            <if test="id != null">
                or id = #{id}
            </if>
            <if test="purchaseOrderId != null and purchaseOrderId != ''">
                or purchase_order_id = #{purchaseOrderId}
            </if>
            <if test="productId != null and productId != ''">
                or product_id = #{productId}
            </if>
            <if test="productNum != null">
                or product_num = #{productNum}
            </if>
            <if test="purchaseMoney != null">
                or purchase_money = #{purchaseMoney}
            </if>
            <if test="depotName != null and depotName != ''">
                or depot_name = #{depotName}
            </if>
            <if test="remark != null and remark != ''">
                or remark = #{remark}
            </if>
        </where>
    </select>


    <!--通过实体作为筛选条件查询-->
    <select id="queryAndByPojo" resultMap="PurchaseOrderDetailsMap">
        select
        <include refid="Base_Column_List"/>
        from purchase_order_details p
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="purchaseOrderId != null and purchaseOrderId != ''">
                and purchase_order_id = #{purchaseOrderId}
            </if>
            <if test="productId != null and productId != ''">
                and product_id = #{productId}
            </if>
            <if test="productNum != null">
                and product_num = #{productNum}
            </if>
            <if test="purchaseMoney != null">
                and purchase_money = #{purchaseMoney}
            </if>
            <if test="depotName != null and depotName != ''">
                and depot_name = #{depotName}
            </if>
            <if test="remark != null and remark != ''">
                and remark = #{remark}
            </if>
        </where>
    </select>
    <!-- 新增所有列 -->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into purchase_order_details(id, purchase_order_id, product_id, product_num, purchase_money, depot_name,
                                           remark)
        values (#{id}, #{purchaseOrderId}, #{productId}, #{productNum}, #{purchaseMoney}, #{depotName}, #{remark})
    </insert>

    <!-- 批量新增 -->
    <insert id="batchInsert">
        insert into purchase_order_details( purchase_order_id, product_id, product_num, purchase_money, depot_name,
        remark)
        values
        <foreach collection="entities" item="item" index="index" separator=",">
            ( #{item.purchaseOrderId}, #{item.productId}, #{item.productNum}, #{item.purchaseMoney},
            #{item.depotName}, #{item.remark} )
        </foreach>
    </insert>

    <!-- 通过主键修改数据 -->
    <update id="update">
        update frameproject.purchase_order_details
        <set>
            <if test="purchaseOrderId != null and purchaseOrderId != ''">
                purchase_order_id = #{purchaseOrderId},
            </if>
            <if test="productId != null and productId != ''">
                product_id = #{productId},
            </if>
            <if test="productNum != null">
                product_num = #{productNum},
            </if>
            <if test="purchaseMoney != null">
                purchase_money = #{purchaseMoney},
            </if>
            <if test="depotName != null and depotName != ''">
                depot_name = #{depotName},
            </if>
            <if test="remark != null and remark != ''">
                remark = #{remark},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from purchase_order_details
        where id = #{id}
    </delete>

    <!-- 总数 -->
    <select id="count" resultType="int">
        select count(*)
        from purchase_order_details
    </select>
    <select id="queryAll" resultMap="PurchaseOrderDetailsMap">
        select
        <include refid="Base_Column_List"/>
        from purchase_order_details p
    </select>
</mapper>
