<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xingji.frameproject.mybatis.dao.SysRoleMenuDao">

    <resultMap type="com.xingji.frameproject.mybatis.entity.SysRoleMenu" id="SysRoleMenuMap">
        <result property="roleId" column="role_id" jdbcType="INTEGER"/>
        <result property="menuId" column="menu_id" jdbcType="INTEGER"/>
    </resultMap>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="SysRoleMenuMap">
        select
        role_id, menu_id
        from frameproject.sys_role_menu
        <where>
            <if test="roleId != null">
                and role_id = #{roleId}
            </if>
            <if test="menuId != null">
                and menu_id = #{menuId}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="" useGeneratedKeys="true">
        insert into frameproject.sys_role_menu(role_id, menu_id)
        values (#{roleId}, #{menuId})
    </insert>

    <insert id="insertBatch" keyProperty="" useGeneratedKeys="true">
        insert into frameproject.sys_role_menu(role_id, menu_id)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.roleId}, #{entity.menuId})
        </foreach>
    </insert>


    <!--通过角色id删除-->
    <delete id="deleteById">
        delete from frameproject.sys_role_menu where  role_id= #{roleId}
    </delete>

</mapper>

